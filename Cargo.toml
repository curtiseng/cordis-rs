[package]
name = "cordis-rs"
version = "0.3.0"
edition = "2024"
rust-version = "1.85"
description = "时空可组合性演算的 Rust 实现：可撤销 effect、响应式 coeffect、fiber 惯性生命周期、声明式配置层"
repository = "https://github.com/curtiseng/cordis-rs"
license = "MIT"
keywords = ["plugin", "effects", "coeffects", "composability", "lifecycle"]
categories = ["rust-patterns", "asynchronous"]

[lib]
# 包名跟仓库对齐，但对外的 crate 名是 cordis。
name = "cordis"

[dependencies]
futures = { version = "0.3.34", default-features = false, features = ["std", "executor"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
serde_yaml_ng = "0.10.0"
slotmap = "1.1.1"

[lints.rust]
missing_debug_implementations = "allow"
unsafe_code = "forbid"

[dev-dependencies]
# 只有测试要 `join!` 来观察两轮对账的交错；库本身不需要 async-await，
# 特性合并只发生在构建本包的测试目标时。
futures = { version = "0.3.34", features = ["async-await"] }
# 文件监听属于宿主而不是库：watch_config 示例演示怎么接。
notify = "8.2.0"
